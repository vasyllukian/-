# from function_course import *
from function_course_no_print import *
import matplotlib.pyplot as plt
# x_0 = (10,10)
x_0 = (-1.2,0)

# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# h_list = [0.1,0.01,0.001,0.0001,0.00001,0.000001]
# kozcf_list = [440,268,348,272,272,272]
# plt.plot(h_list,kozcf_list)
# plt.scatter(h_list,kozcf_list)
# plt.xlabel("Значення h при обчиленні похідних")
# plt.ylabel("КОЗЦФ")
# plt.grid()
# plt.show()

# partan_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# h_list = [0.1,0.01,0.001,0.0001,0.00001,0.000001]
# kozcf_list = [21251,2285,239,239,239,239]
# plt.plot(h_list,kozcf_list)
# plt.scatter(h_list,kozcf_list)
# plt.xlabel("Значення h при обчиленні похідних")
# plt.ylabel("КОЗЦФ")
# plt.grid()
# plt.show()

# mns_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# h_list = [0.1,0.01,0.001,0.0001,0.00001,0.000001]
# kozcf_list = [4112,1671,2659,749,546,1072]
# plt.plot(h_list,kozcf_list)
# plt.scatter(h_list,kozcf_list)
# plt.xlabel("Значення h при обчиленні похідних")
# plt.ylabel("КОЗЦФ")
# plt.grid()
# plt.show()

# partan_mod_l_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# partan_mod_c_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_l_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# partan_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# partan_c_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# mns_l_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# mns_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# mns_c_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')


# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# partan_mod_pr_other(x_0, dsc=1, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')
# partan_mod_pr_other(x_0, dsc=1, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# mns_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# mns_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_mod_pr_norm(x_0, dsc=0, epsilon=0.0001, lim=300)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_mod_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

# partan_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
# print(f'Кількість обчислень цільової функції: {f.calls}')

mns_pr_other(x_0, dsc=0, epsilon=0.0001, lim=1000000)
print(f'Кількість обчислень цільової функції: {f.calls}')
